# Production environment. Copy to `frontend/.env.production` and fill in real values — read by
# `npm run build:production` (`vite build --mode production`). Placeholder value only here.
# Backend API base URL for the production deploy.
VITE_API_URL=https://api.example.com
