# Staging environment. Copy to `frontend/.env.staging` and fill in real values — read by
# `npm run build:staging` (`vite build --mode staging`). Placeholder value only here.
# Backend API base URL for the staging deploy.
VITE_API_URL=https://staging-api.example.com
