# Local development environment.
# Copy to `frontend/.env` — `.env` is gitignored, this file is not.
# Backend API base URL — already falls back to this exact value if unset (src/lib/api.ts, auth.ts, http.ts).
VITE_API_URL=http://localhost:3001
